import { Box, Typography } from "./components";

function NotFound() {
  return (
    <Box display="flex" textAlign="center" height="100%">
      <Typography variant="h1">Oops! We can't find this page</Typography>
    </Box>
  );
}

export default NotFound;
